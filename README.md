# secureview-browser-extension
Repository for secureview browser extension which generates reports for the browser usage by user. Its has local AI model feature to categorise the web url and shows the usage pattern.
